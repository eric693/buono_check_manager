# 每天台灣時間凌晨 3:15 備份，保留 30 份（伺服器系統時區是 UTC，所以要寫明時區）
#   systemctl enable --now buono-backup@staging.timer
[Unit]
Description=Daily backup of buono attendance data (%i)

[Timer]
OnCalendar=*-*-* 03:15:00 Asia/Taipei
Persistent=true

[Install]
WantedBy=timers.target
